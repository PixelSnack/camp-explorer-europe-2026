**BLOCKER | Comparison has no integration contract.** §2 addresses unique “comparison keys,” but not record lookup, selection limits or removal. Comparison code is absent; assume it currently resolves IDs against `allCamps`. **Fix:** introduce a combined `campById` registry exclusively for cross-season operations, never summer counts or filters. Preserve the existing comparison limit across both seasons, label season and dated sessions, retain selections across navigation, and discard stale persisted IDs safely. Test selecting, displaying, booking and removing one camp from each season.

**HIGH | Hash queries leak into summer filters.** App.jsx 590–596 applies query parameters even when the section is winter or unknown. Thus `#winter?category=sports` or `#main-content?search=ski` changes Discover state. Lines 585–586 also leave the current view active when Back reaches an empty hash. **Fix:** validate the section before processing queries, accept filter parameters only for Discover, map empty hash to Home, and preserve unknown-anchor behavior. §3’s “only entry point” is inaccurate: initial hashes and browser history are entry points too. Test all three.

**HIGH | Winter’s filter FAB contradicts the design.** §1 says no winter filters; §4 says the filter FAB works as on Discover. App.jsx 4578 currently excludes winter, correctly. **Fix:** keep it excluded. Render winter directly from its own array, not `filteredCamps`; close any open filter drawer on navigation without clearing summer selections. Test entering winter with search, country, price and age filters already set.

**HIGH | CampCard extraction is a production regression surface.** §4’s four props omit a video callback and may miss other closed-over dependencies. App.jsx 1799 and 1803 hard-code summer into image alt/title text. Featured sorting lives outside the card at 1790. **Fix:** inventory dependencies before extraction; preserve summer output exactly, default season to summer, and parameterize winter wording. Keep the React key on `CampCard`, root `data-camp-card`, classes and wrapper structure unchanged. Supplement the DOM diff with compare, booking, video, badge and image-fallback interaction tests.

**HIGH | “Same per-camp checks” is insufficient.** The supplied validator does not validate IDs, category, required card strings/images, prices or units, date length, paid-slot limits, or five-point evidence. Non-null reviews can pass without `reviewData`. §2’s cross-array collision check would still permit duplicates within winter. **Fix:** use one global ID set, reject duplicate/invalid IDs and winter ID 70, require winter category/season and UI-required fields, and require review evidence. Maintain a checked source/date record for pricing, booking status and each admission criterion, specific to the winter programme and facility, not merely the operator brand.

**HIGH | Winter season validity is deferred too far.** §2 retains the summer `SEASON_YEAR` check; §4 claims “Most winter camps take bookings from September” and that dates represent the “coming winter.” Neither follows from verified open registration. **Fix:** first release should allow winter booking status only as verified `open` or absent, with verification date and explicit session years. Define the winter season independently, for example December 2026 through April 2027, without changing summer’s year constant. Remove the unsupported “most” claim and do not reuse SeasonNotice’s summer publication schedule, App.jsx 113. Defer the published-date badge, not year clarity.

**HIGH | Protected SEO assets lack protection tests.** The validator checks only that the title contains `SEASON_YEAR`; its country check succeeds if the expected phrase appears anywhere. Neither proves that protected metadata or the rendered H1 stayed unchanged. **Fix:** snapshot the exact approved title, description, OG tags and home H1; assert summer counts remain unchanged and inspect the rendered home DOM. Preserve the current summer heading hierarchy and prominence.

**HIGH | Mobile gates miss the likely failures.** App.jsx 897–905 switches to a non-wrapping desktop nav at `md`; another item can overflow near that breakpoint despite passing 320, 390 and wide desktop. Headless Chrome does not cover iOS Safari. **Fix:** add breakpoint-adjacent widths and real Safari or WebKit checks, including short landscape screens. Test the longest winter badges, dates and prices, featured cards with booking badges, open menus and comparison trays. Make the menu vertically scrollable when needed; verify safe-area clearance, keyboard focus and no horizontal overflow.

**HIGH | Analytics changes are not fully wired.** §6 adds `camp_season`, but §4 leaves Home’s card copy untouched and omits video handling from the extracted component contract. Events could become inconsistent across placements or fire twice. **Fix:** centralize booking/video instrumentation and outbound URL construction, using `camp.season ?? 'summer'` everywhere, including Compare and Home handlers without changing Home markup. Preserve existing UTM values and URL queries, and keep popup opening synchronous for iOS. Register the GA4 event-scoped custom dimension. Verify exactly one event per action; measure winter-view visits separately without duplicating automatic page views.

**MEDIUM | Premium reuse does not guarantee priority or capacity.** §6 says Premium works unchanged, but the priority sort is a Discover-parent operation, App.jsx 1790. **Fix:** explicitly apply stable featured-first sorting to winter and validate at most three `featured` winter records, regardless of price tier. Keep paid placement distinct from the `premium` price class: a budget-priced featured listing still consumes a paid slot. Confirm badge wording, video eligibility and outbound attribution match the sold package.

**MEDIUM | Scroll-to-last-camp behavior is unspecified.** §4 promises reuse, but its handler and observers are not supplied. A shared selector or summer-derived endpoint could target a stale or incorrect card after routing. **Fix:** scope lookup to the active grid and target its last rendered card after featured sorting. Retarget observers after section/list changes, clean them up, and hide the control for empty grids or obstructing overlays. Test Discover → Winter → Compare → Back, including filtered Discover results.

**MEDIUM | Winter SEO expectations exceed the available surface.** §5 cannot create an independently indexable winter landing page. A short, subordinate teaser plus one FAQ is a limited relevance change, not automatically harmful, but winter rankings and unchanged summer rankings are not guaranteed. **Fix:** keep the teaser below the FAQ, concise and visually subordinate; avoid repeated winter query variants or site-wide winter branding. Monitor the same URL’s summer query groups, CTR, position and displayed title against a pre-release baseline, accounting for seasonality. Unchanged metadata does not prevent Google rewriting titles or snippets.

**MEDIUM | ItemList and BreadcrumbList need different treatment.** §5’s eighth ItemList entry is reasonable **if** the existing list describes navigation sections, with unique positions and matching `numberOfItems`. It must not change camp counts or imply a separately indexed page. Full JSON-LD was not supplied. **Fix:** verify that meaning before editing. Do not append Winter as another hierarchical ancestor in the root page’s static BreadcrumbList. Add the runtime Home → Winter breadcrumb through App.jsx 431’s switch; keep root structured breadcrumbs truthful to Home.

**MEDIUM | Rollback is incomplete.** §8 calls data and validators “inert,” although validators execute on every build; reverting the third commit also leaves the Discover extraction live. **Fix:** document two rollback paths: winter exposure only, and extraction regression. Tag the known-good release and test both reverts, including shared handlers. Review the complete diff, not only App.jsx: CampCard, validators, data, analytics and JSON-LD carry substantial risk.

**MEDIUM | Publication gating is inconsistent.** §4 gates only the menu at four verified camps; teaser, FAQ, schema and defensive-domain routing are unspecified. One to three listings and the dated empty-state message are unresolved. **Fix:** use one publication flag for all promotional entry points and related schema. Keep direct unpublished hashes useful but non-promotional. Replace “autumn 2026” with an undated fallback. Candidate names do not count as verified inventory.

**LOW | Defensive-domain behavior needs an implementation decision.** §1 merely says it “points” at winter. **Fix:** use a permanent redirect to the canonical summer domain’s `/#winter`, preserve relevant attribution parameters, test the fragment destination, and avoid a mirrored or framed indexable site.

**Checked and clean:** Separate `winterCamps` plus a season discriminator is preferable to merging into `allCamps`; the supplied summer filters, countries and stats remain isolated. Winter need not join `KNOWN_CATEGORIES`. Existing badge rendering accepts `open` and absence without a new published-year label. The unchanged single-URL sitemap, runtime whitelist/breadcrumb plan, null-review default, five-point launch gate and FAQ/JSON-LD equality requirement are sound. These are source/spec checks, not executed build or browser results.